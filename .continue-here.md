---
project: claude-ipc (collab tool)
instance: claude-ipc
last_updated: 2026-04-01T21:30:00Z
status: active
---

<current_state>
collab worker implementation in progress. New event-driven headless harness replaces /loop polling.
Changes pending: git pull && ./build.sh on kali to pick up worker feature.
</current_state>

<completed_work>

- Root cause found: collab stream output doesn't inject into Claude sessions — only cron/loop does.
  Broadcast to all workers, updated README (ef151d3), all 6 worker CLAUDE.md files updated (d4builder commit 23f2393).
- collab init (init.rs) updated to generate correct session start — /loop + stream (4749dee).
- Web app SSE reconnect fix: onerror now does exponential backoff reconnect (300ce6a).
- Auth bug fixed: URL-decode %3D→= in query token param so EventSource auth works (a268195).
- Security finding: SSE query param token documented in checklist (44846d4). @redteamer flagged CWE-598.
  Acknowledged, cookie auth recommended long-term, low risk for local dev.
- All worker .collab.toml files updated: openrouter and claude-ipc added to all recipient lists.
- /Users/operator/code/openrouter/.collab.toml created for @openrouter (no token — uses COLLAB_TOKEN env).
- @project-manager recipients updated to include openrouter and claude-ipc.
- @builder .collab.toml: token was missing, added.
- collab-web/token-report.html deleted (was @openrouter's report, not committed).

</completed_work>

<remaining_work>

- Kali needs: git pull && ./build.sh then restart collab-server — picks up auth fix (a268195).
  Without this, web app SSE gets 401 because %3D token isn't decoded by old binary.
- LLM platform configs: @jabberwock wants collab init to generate platform-specific agent setup files:
  CLAUDE.md for Claude Code, .cursorrules for Cursor, system prompt for OpenRouter/Gemini/Ollama.
  Awaiting @jabberwock confirmation of approach (asked, no response yet).
- @jabberwock also mentioned Cursor CLI support and Gemini support — both pending confirmation.

</remaining_work>

<decisions_made>

- cron/loop is the Claude agent wake-up mechanism, collab stream is for web dashboard + human terminals only.
  This is now documented in README, all CLAUDE.md files, and collab init output.
- SSE query param token: known limitation (EventSource can't set headers). Documented as accepted risk.
  Long-term fix: fetch + ReadableStream or HttpOnly cookie auth.
- Worker-specific .collab.toml files stay OUT of the repo (user-specific). Platform config templates
  (CLAUDE.md, .cursorrules, etc.) will be generated by collab init per @jabberwock's direction.

</decisions_made>

<blockers>

- Kali server restart pending (human action) — blocks web app SSE auth fix going live.

</blockers>

<context>
Working as @claude-ipc. Token in /Users/operator/code/claude-ipc/.collab.toml.
Cron job fires collab list every minute (injects as user prompt).
Background stream running PID 10406.
d4builder workers all aligned on cron-first approach.
</context>

<next_action>
Run collab list to check for messages. Confirm kali has restarted with auth fix.
Then wait for @jabberwock to confirm LLM platform config approach before building.
</next_action>
